# In Docker: leave these empty — nginx proxies /api/* and /ws/* to the backend container.
# For local dev without Docker: set to http://localhost:8000 and ws://localhost:8000
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
